import {StyleSheet} from 'react-native';

const styles = StyleSheet.create ({
  calendarContainer: {
    borderWidth: 1,
    borderColor: 'gray',
    height: 350,
    padding: 5,
  },
  buttonConiainer:{
    marginTop: 10,
    flexDirection: 'row',
    justifyContent: 'space-evenly',
  },
});
